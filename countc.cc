#include <iostream>
#include <string>
#include <sstream>
using namespace std;

/*
int main()
{
    string word = "llllllllll";
    string temp;
    string result="";
    int count = 0;
    char c = 'l';
    int length = word.length();

    for(int i=0; i<length; ++i)
    {
        if(word[i] == c)
        {
            ++count;
            ostringstream convert;
            convert << count;
            temp = convert.str();
            result+= temp;
        }
        else
        {
            result += word[i];
        }
    }
    cout << result << endl;
}*/
